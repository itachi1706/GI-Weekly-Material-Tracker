node_modules/
out/
dist/

# Vitest coverage output + reports
coverage/
*.lcov
.vitest/

# The Flutter repo root ignores `build/` globally, but here build/ is our electron-builder
# buildResources dir (app icon + afterPack signing hook) and must be tracked.
!build/
!build/**

# Local, uncommitted reference copy of the dataset (selected at runtime via the folder picker)
dataset/

.DS_Store
*.log
